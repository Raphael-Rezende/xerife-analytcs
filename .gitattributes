# Normalize line endings in the repository (recommended for cross-platform teams)
* text=auto eol=lf

# Keep Windows scripts with CRLF if we ever add them
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
