/**
 * Página inicial do produto (rota: /inicio).
 * Objetivo: validar o App Router e ter um ponto de entrada claro do MVP.
 */
export default function InicioPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Xerife Analytics</h1>
      <p>Rota /inicio funcionando ✅</p>

      {/* TODO(MVP): adicionar login e seleção de workspace */}
    </main>
  );
}